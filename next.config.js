/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // domains: ["blog.kakaocdn.net", "cdn.pixabay.com"],
    domains: ["image.msscdn.net"],
  },
};

module.exports = nextConfig;
